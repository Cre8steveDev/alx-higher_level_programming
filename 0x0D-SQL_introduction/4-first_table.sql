-- Introduction to SQL | MySQL
-- Creating a Table; Assuming the database is provided as command
-- line argument when the mysql server is launched

CREATE TABLE IF NOT EXISTS first_table (
	id INT, 
	name VARCHAR(256)
	);