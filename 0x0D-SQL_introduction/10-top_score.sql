-- Sorting a result from table in descending order
-- Using the ORDER BY a particular field or fields
-- You can pass multiple ordering values

SELECT score, `name` FROM second_table ORDER BY score DESC;